# Evaluation Compatibility Matrix

This matrix links each worklist area (except the deliberately late CoreCLR
section) to the upstream `dotnet/msbuild` test that informed it. “Representative”
means the fixture intentionally covers a small supported subset; it does not
claim full upstream parity.

References in this table are pinned to `dotnet/msbuild` commit
[`e02ab1fa8eae232bfeb965347daf01ddc7cb496d`](https://github.com/dotnet/msbuild/tree/e02ab1fa8eae232bfeb965347daf01ddc7cb496d).

| Area | Upstream test file + method | Local port / fixture | Status | Notes |
| --- | --- | --- | --- | --- |
| Basic property expansion | [`Evaluator_Tests.AllEvaluatedProperties`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L1931) | `evaluation::tests::upstream_all_evaluated_properties_uses_each_preceding_value`; `fixtures/evaluation/basic` | ported | Assignment values observe only preceding state. |
| Property conditions | `src/Build.UnitTests/Evaluation/Evaluator_Tests.cs` — `UsePropertyInCondition` | `fixtures/evaluation/basic` (`ConditionalValue`) | representative | Exercises a true property condition. |
| Last assignment / document order | [`Evaluator_Tests.AllEvaluatedProperties`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L1931) | unit port + `fixtures/evaluation/basic` (`AllEvaluated`) | ported | Includes skipped assignments and reassignment using the preceding value. |
| Self, before-set, and mutual references | [`Evaluator_Tests.SetPropertyToItself`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L657), [`UsePropertyBeforeSet`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L745) | `evaluation::tests::upstream_self_reference_before_set_and_mutual_references_are_single_pass`; fixture (`SetToItself`, `UsedBeforeSet`) | ported | MSBuild does not fixed-point-expand stored values. Raw environment/global values containing `$(...)` remain literal. Actual expression/function nesting is bounded; parentheses in literal text are opaque. Warning emission is deferred. |
| Global-property precedence | [`VerifyGlobalPropertyOverridesIfNoTreatAsLocalProperty`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L2986), [`VerifyOnlySpecifiedPropertiesOverridden`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L3365) | `evaluation::tests::raw_initial_values_are_literal_and_global_properties_win`; fixture (`GlobalChoice`) | partial | Global immutability and specified-property behavior are ported. `TreatAsLocalProperty`, which is also exercised by the latter upstream test, is explicitly deferred. |
| Environment properties | [`MSBuildExtensionsPathWithEnvironmentOverride`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L2498) | injected-environment unit port; fixture (`MSBuildExtensionsPath`) | ported | Environment is snapshotted once per context; project properties can override ordinary environment properties. |
| Reserved project/current-file and toolset properties | [`ReservedProjectProperties`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L2797), [`MSBuildThisFileProperties`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs#L1361) | `evaluation::tests::upstream_reserved_project_properties_and_current_file_properties`; `host_resolved_sdk_and_toolset_match_the_repo_global_json`; fixture reserved/current-file queries | representative | The upstream reserved-name set is protected. Project paths retain lexical absolute spelling; `MSBuildThisFile*` exists only during active file evaluation and queries empty afterward. Host-derived tools/bin/version/runtime values have direct semantic-fixture parity. Edge UNC behavior is deferred. |
| Path functions | `src/Build.UnitTests/Evaluation/Expander_Tests.cs` — `PropertyFunctionStaticMethodGetPathOfFileAbove` | `src/expression.rs` tests; `fixtures/evaluation/review-findings` | ported | Native preprocessing path-function coverage. Static and instance operands are decoded once, and results are escaped before reinsertion; the fixture covers an escaped slash passed to `Path.GetFileName` and semicolon-bearing results. |
| Registry properties | `src/Build.UnitTests/Evaluation/Expander_Tests.cs` — `RegistryPropertyString` | — | backlog | Platform-dependent behavior. |
| Boolean condition evaluation | `src/Build.UnitTests/Evaluation/Evaluator_Tests.cs` — `VerifyConditionsInsideOutsideTargets` | `src/expression.rs` tests; `fixtures/evaluation/conditions` | ported | Includes grouping, `And`/`Or` precedence and lazy operand/property-function expansion, and the exact case-insensitive true aliases (`true`, `on`, `yes`, `!false`, `!off`, `!no`) and false aliases (`false`, `off`, `no`, `!true`, `!on`, `!yes`). Whitespace and empty strings are not coerced to booleans. |
| Condition diagnostics | `src/Build.UnitTests/Evaluation/Evaluator_Tests.cs` — `BadConditional` and selected `ExpressionTreeExpression_Tests.EvaluateAVarietyOfErrorExpressions` cases | `expression::tests::test_malformed_conditions_are_errors`; `upstream_relational_and_condition_expression_subset` | representative | The additional ported errors are `1 > 'x'`, `x1<=1`, `1<=x`, `1<=1<=1`, and `1>=$(b)`. |
| Condition intrinsics | `src/Build.UnitTests/Evaluation/Evaluator_Tests.cs` — `UsePropertyInCondition`; `MSBuild.IntrinsicFunctions.IsOSPlatform` | `expression::tests::evaluates_condition_intrinsics`; `is_os_platform_matches_msbuild_platform_names_case_insensitively`; `fixtures/evaluation/conditions` | ported | `Exists` and `HasTrailingSlash` were already complete. `IsOSPlatform` is available through `$([MSBuild]::IsOSPlatform(...))`, accepts case-insensitive `Windows`, `Linux`, and `OSX`, returns false for whitespace and unknown values, and errors for an empty value. |
| Relational/version comparisons | [`ExpressionTree_Tests.RelationalTests`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/ExpressionTree_Tests.cs) and selected [`ExpressionTreeExpression_Tests`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/ExpressionTreeExpression_Tests.cs) true/false cases | `expression::tests::upstream_relational_and_condition_expression_subset`; `numeric_coercion_matches_msbuild_double_and_int32_hex_behavior`; `fixtures/evaluation/conditions` | representative | Ports every `RelationalTests` assertion plus focused boolean, short-circuit, version, overflow, signed-hex, and 2^53-rounding regressions. Decimal coercion uses finite invariant-culture IEEE-754 doubles; hexadecimal coercion uses signed 32-bit values (`0xFFFFFFFF == -1`) and rejects values outside 32 bits. Equality attempts numeric, then boolean, then case-insensitive string comparison, so numeric overflow falls back to string equality; relational numeric overflow is an error. Relational coercion attempts number/number, version/version, then mixed number/version ordering, using .NET-style two-to-four-part versions. This does not claim all upstream parser syntax permutations are ported. |
| Basic item include and identity | `src/Build.UnitTests/Evaluation/Expander_Tests.cs` — `ItemIncludeContainsMultipleItemReferences` | `evaluation::tests::upstream_item_expression_grammar_and_function_chaining`; `fixtures/evaluation/basic`; `fixtures/evaluation/items-operations` | ported | Multiple item references observe the item snapshot at their source operation and preserve identity, metadata, and order. |
| Basic custom and well-known metadata | [`Expander_Tests.DirectItemMetadataReferenceShouldBeCaseInsensitive`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Expander_Tests.cs), `WellKnownMetadataReferenceShouldBeCaseInsensitive` | `evaluation::tests::upstream_metadata_and_well_known_references_are_case_insensitive`; `evaluation::tests::reserved_metadata_is_rejected_case_insensitively`; `fixtures/evaluation/escaping-metadata` | ported | Custom/default and non-timestamp well-known metadata use indexed case-insensitive lookup. Reserved well-known names are rejected on items and item definitions. Timestamp value calculation remains backlog. |
| Item transforms and separators | `Expander_Tests.ItemFunctionChainingWithWhitespaceBeforeArrow`, `ExpandItemVectorFunctionsChained1`, `ExpandItemVectorFunctionsItemSpecModifier`; `EscapingInProjects_Tests.ItemTransformContainingSemicolon`; direct reviewer repros | `evaluation::tests::upstream_item_expression_grammar_and_function_chaining`; `upstream_expand_item_vector_functions_item_spec_modifier`; `upstream_item_transform_containing_semicolon`; `reviewed_item_pipeline_semantics_match_direct_msbuild`; `fixtures/evaluation/items-operations`; `fixtures/evaluation/item-pipeline` | ported common surface | Literal/metadata templates retain null correlation entries between stages, while final item materialization drops them. Explicit separators, including `;`, produce one source-less atomic item. Identity-preserving transforms retain `RecursiveDir`; changed identities retain source custom/default metadata but recompute well-known path metadata. |
| Intrinsic item functions | `Expander_Tests.ExpandItemVectorFunctionsCount1/2`, `CountOperatingOnEmptyResult1/2`, `BuiltIn1`, `BuiltIn3`, `AnyHaveMetadataValue`, `HasMetadata`, `WithoutMetadataValue`, `ClearMetadata`, `Exists`, `DirectoryName`, and `Combine`; direct reviewer repros | `evaluation::tests::upstream_common_item_functions_and_empty_results`; `reviewed_item_pipeline_semantics_match_direct_msbuild`; `expression::tests::item_function_arguments_expand_properties_and_current_metadata_before_splitting`; `ordinal_ignore_case_key_matches_dotnet_utf16_edge_cases`; `fixtures/evaluation/item-pipeline` | ported common surface | Scalar outputs can feed later stages. Function arguments expand properties/current metadata before comma parsing without recursively expanding nested item vectors. `ClearMetadata`, `Combine`, and explicit separators clear source provenance. Distinct functions compare escaped identities with .NET ordinal behavior (`%41` differs from `A`, `K` differs from `K`, and `σ` equals `ς`). Rust strings cannot represent .NET's unpaired UTF-16 surrogates, which are also invalid in XML; valid scalar values use one-to-one invariant ordinal folding. Remaining: `GetPathsOfAllDirectoriesAbove`, timestamp metadata, and allowlisted string methods. |
| `Exclude`, `Remove`, and `Update` | `ItemEvaluation_Tests.IncludeShouldPreserveIntermediaryReferences`, `ExcludeSeesIntermediaryState`, `RemoveShouldPreserveIntermediaryReferences`, `UpdateShouldPreserveIntermediaryReferences`, `RemoveRespectsItemTransform`, `UpdateRespectsItemTransform`, and `MultipleInterItemDependenciesOnSameItemOperation` | `evaluation::tests::upstream_include_and_exclude_observe_intermediary_state`; `upstream_remove_and_update_preserve_intermediary_references`; `upstream_remove_and_update_respect_item_transforms`; `upstream_multiple_inter_item_dependencies_on_same_operation`; fixture | ported | Operations are ordered, match current items without mutating copied snapshots, evaluate conditions/metadata in item context, and use hash-indexed literal selectors plus compiled wildcard selectors. Target-only mutation attributes remain out of scope. |
| Wildcards and recursive globs | `ItemEvaluation_Tests.DifferentExcludesOnSameWildcardProduceDifferentResults`; [`EscapingInProjects_Tests.EscapedWildcardsShouldNotBeExpanded`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/EscapingInProjects_Tests.cs); `ItemGlobs_Tests.PatternsWithPercentEncodingRoundTripAndMatchGetAllGlobs` | `evaluation::tests::upstream_different_excludes_and_recursive_glob_metadata`; `percent_patterns_round_trip_once_and_escaped_wildcards_stay_literal`; `fixtures/evaluation/items-operations` | ported eager surface | Eager expansion is deterministic and root-project-relative, caches identical walks, computes `RecursiveDir`, and treats escaped wildcards/brackets literally. Synthetic glob reporting and opt-in lazy wildcard retention remain backlog. |
| Item evaluation scale | `ItemEvaluation_Tests.LongIncludeChain`; `LazyWildcardExpansionDoesNotEvaluateWildCardsIfNotReferenced` | `evaluation::tests::upstream_long_include_chain_handles_ten_thousand_items_iteratively`; `benchmark_scale_remove_and_update_use_indexed_exact_matching`; `upstream_lazy_wildcard_case_is_tracked_as_an_eager_deviation` | ported / tracked | The 10,000-include port prevents recursive stack growth; the 10,000-item selector regression guards indexed matching. The lazy test is explicitly tracked as an eager-behavior deviation, not claimed as parity. |
| Item definition ordering | [`Evaluator_Tests.ItemDefinitionPredecessorToItemDefinition`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs), `ItemDefinitionPredecessorToItem`, `AllEvaluatedItemDefinitionMetadata` | `evaluation::tests::upstream_item_definition_predecessor_and_all_evaluated_metadata`; `fixtures/evaluation/escaping-metadata` | ported | Definitions and metadata conditions evaluate in document order. Later/case-changed definitions override earlier values; item snapshots share immutable default layers and explicit metadata wins. |
| Relative import resolution and source position | [`Evaluator_Tests.VerifyConditionsInsideOutsideTargets`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs) (file-based import/current-file subset) | `evaluation::tests::imports_observe_parent_state_and_evaluate_at_source_position`; `evaluation::tests::imported_items_use_root_directory_and_literal_bracket_imports`; `fixtures/evaluation/review-findings` | representative | Import and group conditions see the importing lexical file and preceding state. Relative item path metadata uses the root project directory, while `DefiningProject*` identifies the declaring import. Bracketed import names are literal. |
| Import duplicates/cycles | [`Evaluator_Tests.ImportsOnlyIncludedOnce`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs) and [`Evaluator_Tests.RejectCircularImportsWithCircularImports`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs) | `evaluation::tests::upstream_imports_only_included_once_uses_normalized_lexical_identity`; `circular_imports_are_diagnosed_and_skipped_by_default`; Windows-only `import_identity_is_lexical_and_does_not_resolve_symlinks` | partial | Duplicate suppression and cycle detection use normalized lexical full paths (case-insensitive on Windows), without symlink resolution. Default loading warns and skips a circular import. The cited `RejectCircularImportsWithCircularImports` test instead explicitly passes `ProjectLoadSettings.RejectCircularImports`; that strict public load setting is not exposed yet. The symlink test skips if Windows symlink privilege is unavailable. |
| Conditional import and import glob | [`Evaluator_Tests.VerifyLoadingImportScenarios`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs) | `evaluation::tests::upstream_verify_loading_import_scenarios_handles_conditions_and_sorted_globs`; `fixtures/evaluation/imports-project-structure` | representative | Conditions are source-position-aware, false conditional missing imports are ignored, and wildcard matches are lexical-sort deterministic. |
| `ImportGroup` conditions | [`Evaluator_Tests.VerifyConditionsInsideOutsideTargets`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs) (import-group subset) | `evaluation::tests::upstream_imports_only_included_once_uses_normalized_lexical_identity`; `fixtures/evaluation/imports-project-structure`; semantic and preprocess parity runners | ported | The group condition gates each child import; each child condition is then evaluated against the same importing-file context. |
| `Choose` / `When` / `Otherwise` | [`Evaluator_Tests.AllEvaluatedItems`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs) | `evaluation::tests::upstream_all_evaluated_items_choose_selects_only_the_first_true_branch`; `self_closing_when_selects_its_empty_branch`; `choose_structure_is_validated_even_in_inactive_branches`; `fixtures/evaluation/imports-project-structure` | representative | First true `When` wins, including an empty self-closing branch; otherwise runs only when no `When` does. Nested project-level choices are evaluated in order. All branches validate `When`/`Otherwise` order, cardinality, and allowed project-evaluation children before selected content is evaluated. Target-body choices remain backlog. |
| Implicit SDK imports and discovery | `src/Build.UnitTests/Evaluation/ProjectSdkImplicitImport_Tests.cs` — `SdkImportsAreInLogicalProject` | `evaluation::tests::implicit_sdk_imports_are_evaluated_before_and_after_the_project`; `top_level_sdk_elements_import_all_props_and_targets_in_declaration_order`; repo `global.json` host-resolution test | representative | `Project@Sdk` and top-level `Sdk Name/Version` forms import props/targets in declaration order. Cached `dotnet --info` resolution honors `global.json`; explicit `MSBuildSDKsPath` changes SDK lookup without redefining the active toolset. NuGet acquisition for versioned third-party SDKs and full installed-SDK evaluation remain deferred. |
| Preprocessed aggregate source | `src/Build.UnitTests/Evaluation/Preprocessor_Tests.cs` — `Single` | `scripts/compare-preprocess.ps1` | representative | Raw and line-normalized outputs are retained. |
| Percent escaping | [`EscapingInProjects_Tests.CanGetCorrectListOfItemsWithSemicolonsInThem`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/EscapingInProjects_Tests.cs), `CanGetCorrectListOfItemsWithSemicolonsInThem2`, and `ItemTransformContainingSemicolon`; direct `Combine('%3B')` repro | `evaluation::tests::upstream_escaping_projects_preserves_semicolon_items`; `upstream_item_transform_containing_semicolon`; `reviewed_item_pipeline_semantics_match_direct_msbuild`; `fixtures/evaluation/item-pipeline` | ported | Escaped source is retained through splitting/classification, transforms, distinct comparison, and intrinsic arguments/results: raw metadata lists split only at the projection boundary, authored `%3B` stays one identity, and `%2512` decodes once. |
| XML text, CDATA, entities, and DTD safety | [`Evaluator_Tests.VerifyDTDProcessingIsDisabled`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs), `Preprocessor_Tests.CData` | `evaluation::tests::xml_entities_cdata_and_whitespace_decode_once_and_dtd_is_rejected`; fixture (`XmlEntity`, `XmlCData`, `EntityMetadata`, `CDataMetadata`) | ported | quick-xml performs XML decoding separately from one MSBuild escape layer. Text whitespace and CDATA are preserved; DTD/external entity declarations are rejected before resolution. |
| Case-insensitive lookup | [`Evaluator_Tests.ItemPredecessorToItemWithCaseChange`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/Evaluator_Tests.cs), `Expander_Tests.DirectItemMetadataReferenceShouldBeCaseInsensitive`, `WellKnownMetadataReferenceShouldBeCaseInsensitive` | `evaluation::tests::upstream_item_and_metadata_case_changes_find_predecessors`; metadata reference port; `indexed_item_and_metadata_lookup_scales_without_default_clones`; fixture | ported | Properties, item types, custom/well-known metadata, and functions use folded indexed lookup while preserving first-authored casing/order. |
| Semicolon splitting and empty values | [`SemiColonTokenizer_Tests.TokenizeExpression`](https://github.com/dotnet/msbuild/blob/e02ab1fa8eae232bfeb965347daf01ddc7cb496d/src/Build.UnitTests/Evaluation/SemiColonTokenizer_Tests.cs); direct empty-transform chaining repro | `escaping::tests::upstream_semicolon_tokenizer_tokenize_expression`; `evaluation::tests::reviewed_item_pipeline_semantics_match_direct_msbuild`; both escaping-project ports; `fixtures/evaluation/item-pipeline` | ported | List tokenization removes empty/whitespace materialized entries, but transform pipelines retain null correlation entries until a stage such as `Count` consumes them. |
